# Orqforge Infrastructure

Infrastructure assets live here:

- `caddy/` for ingress configuration
- `compose/` for local orchestration
- `scripts/` for smoke tests and local verification

The implementation goal is a single `docker compose up` bringing the system up end-to-end on a clean machine.

