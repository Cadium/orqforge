# Orqforge Shared Contracts

This package will hold narrow shared contracts only:

- API DTO types
- status enums that need frontend/backend alignment
- validation helpers only when they materially reduce duplication

It should not become a dumping ground for unrelated utilities.

