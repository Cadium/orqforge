# Orqforge Hello Node

This sample app will be the first deployment target used to prove that Orqforge can:

- materialize source
- build an image with Railpack
- run a container with Docker
- expose it behind Caddy
- surface logs in the Orqforge UI

The app itself should stay intentionally simple so the platform behavior remains the focus.
